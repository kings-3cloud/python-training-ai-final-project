### Step 10 — End-to-end run

```bash
Everything is built. Give me:
1. How to configure the MCP server in Foundry Toolkit so the agent can call the tools
2. The command to start the Flask app
3. A sample conversation to test all three tools end-to-end (fetch a URL, generate a quiz from it, save a score)
4. What to check in `data/progress.json` to confirm it's working
```