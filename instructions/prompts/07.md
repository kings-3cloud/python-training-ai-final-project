### Step 7 — Build the Flask web app

```bash
In `study-assistant/agent/app.py` and `templates/index.html`, build a simple Flask web app.

Requirements for app.py:
- Single `/` route serving index.html
- POST `/chat` endpoint accepting `{ "message": "..." }` JSON and returning `{ "response": "..." }`
- POST `/reset` endpoint that resets the conversation
- Use the AgentClient from agent_client.py

Requirements for index.html:
- Clean, minimal UI (you can reference the style from `computer-history-client/templates/index.html`)
- Text input + Send button
- "New Session" button that calls /reset
- Chat history displayed above the input
- Show a loading spinner while waiting for a response

Show me all three files (app.py, index.html, style.css).
```