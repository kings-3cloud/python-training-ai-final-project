### Step 6 — Build the Agent client

```bash
In `study-assistant/agent/agent_client.py`, implement an `AgentClient` class following the exact same pattern as `..\ai-final\computer-history-client\agent_client.py`.

Differences for Study Assistant:
- The system context should describe it as a study assistant that can fetch content, generate quizzes, and track progress
- Keep multi-turn conversation history (last 5 exchanges instead of 3)
- Add a method `reset_session()` that clears conversation history
- Load AGENT_ENDPOINT and auth from .env using the same DefaultAzureCredential pattern

Show me the full file.
```

