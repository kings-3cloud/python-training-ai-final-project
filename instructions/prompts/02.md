### Step 2 — Scaffold the project folders and files

```bash
Create the folder structure for `..\ai-final\study-assistant\` with these empty files:

study-assistant/
  mcp_server/
    server.py          ← MCP server with the three tools
    tools/
      fetch_url.py     ← fetch_url_content tool
      quiz.py          ← generate_quiz tool
      progress.py      ← save_progress tool
    __init__.py
  agent/
    agent_client.py    ← connects to Foundry-hosted agent, same pattern as computer-history-client
    app.py             ← Flask web app
    templates/
      index.html
    static/
      style.css
      script.js
  data/
    progress.json      ← stores user quiz progress (start empty: {})
  .env.example         ← template for env vars (no real values)
  requirements.txt
  README.md

Create all these files with placeholder content only (no real logic yet). Add a one-line comment in each .py file describing what it will do.
```