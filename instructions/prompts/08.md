### Step 8 — Wire up `requirements.txt` and `.env`

```bash
Generate the complete `study-assistant/requirements.txt` for the project.

Include:
- flask
- httpx
- PyPDF2
- openai (>=2.38.0)
- azure-identity (>=1.15.0)
- python-dotenv
- mcp (the Python MCP package, latest)
- markdown
- bleach

Also generate the complete `.env.example` file with every variable the project needs, with comments explaining each one. Do NOT put real values in it.
```