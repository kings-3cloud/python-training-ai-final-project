### Step 4 — Wire up the MCP Server

```bash
In `study-assistant/mcp_server/server.py`, create a Python MCP server using the `mcp` package (FastMCP style).

- Import and register all three tools from the `tools/` subpackage: fetch_url_content, generate_quiz, save_progress
- Each tool should be decorated with `@mcp.tool()` with a proper docstring describing its parameters
- The server should run with `mcp.run(transport="stdio")` so it can be connected to Foundry Toolkit or any MCP host
- Add a `if __name__ == "__main__"` entry point

Show me the full server.py file.
```