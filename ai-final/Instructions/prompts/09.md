### Step 9 — Install dependencies and test each tool

```bash
I''m in `..\ai-final\` with the `.venv` already active.

Give me the exact terminal commands to:
1. Install all packages from `study-assistant/requirements.txt` into the existing .venv
2. Test `fetch_url.py` standalone with a sample URL
3. Test `progress.py` standalone with dummy data
4. Start the MCP server and confirm it loads without errors

Run them one at a time so I can check each result.
```