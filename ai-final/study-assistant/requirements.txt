# ── MCP Server ────────────────────────────────────────────────────────────────
mcp[cli]>=1.0.0          # MCP framework + CLI (mcp dev, mcp run)
httpx>=0.27.0            # Async-capable HTTP client for fetch_url_content
PyPDF2>=3.0.0            # PDF text extraction
beautifulsoup4>=4.12.0   # Optional HTML parsing helpers

# ── Agent app (Flask client) ───────────────────────────────────────────────────
flask>=3.0.0             # Web framework
openai>=2.38.0           # OpenAI-compatible client for Foundry endpoints
azure-identity>=1.15.0   # DefaultAzureCredential for Foundry auth
python-dotenv>=1.0.0     # Loads .env into os.environ
markdown>=3.7            # Markdown → HTML conversion
bleach>=6.1.0            # HTML sanitisation
